package Test;
import Action.HarvestAction;
import Animal.Animal;
import Animal.Create;
import Base.CommonFarmBuilder;
import Base.Director;
import Building.Farm;


public class FeederTest {
	public static void main() {
		// test Visitor & Observer & Mediator
		/**
		 * Because the implementation of the Visitor & Observer & Mediator pattern 
		 * relies too much on other project components, 
		 * we will test the Visitor & Observer & Mediator pattern in the MainTest class.
		 */
	}
}